<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.dash.ai.infrastructure.TutorConversationMapper">

    <insert id="save" parameterType="com.ssafy.dash.ai.domain.TutorConversation" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tutor_conversation (user_id, session_id, role, content, problem_number, created_at)
        VALUES (#{userId}, #{sessionId}, #{role}, #{content}, #{problemNumber}, NOW())
    </insert>

    <select id="findByUserIdAndSessionId" resultType="com.ssafy.dash.ai.domain.TutorConversation">
        SELECT *
        FROM tutor_conversation
        WHERE user_id = #{userId}
        AND session_id = #{sessionId}
        ORDER BY created_at ASC
    </select>

</mapper>
